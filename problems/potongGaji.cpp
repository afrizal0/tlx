#include <bits/stdc++.h>
using namespace std;
int main(){
	int T;
	cin >> T;
	for(int i=0; i<T; i++){
		int N, M;
		cin >> N >> M;
		int arr[N];
		for(int j=0; j<N; j++){
			cin >> arr[j];
		}
		int temp = 0;
		for(int k=0; k<N; k++){
			if(arr[k]>temp){
				temp = arr[k];
			}
		}
		if(temp > 
		
	}
}
