#include <bits/stdc++.h>
using namespace std;

vector<vector<int, char> v;
string cek(int n, int m, string id) {
	for(int j=0; j<n; j++) {
		for(int l=0; l<4; l++){
			string ids;
			int score;
			cin >> ids >> score;
		}
	}
	
	for(int j=0; j<n-1; j++){
		for(int l=1; l<4; l++){
			if(
			
}


int main() {
	int K;
	cin >> K;
	for(int i=0; i<K; i++) {
		int N, M;
		string ID;
		cin >> N >> M;
		cin >> ID;
}

/*
 * cek Aarr[0] == ID 
 * 
 * if cek == M and Aarr[n] < 
 * acc
 * temp;
 * A[len-1] --> B[len-1] --> 
 * ==> temp
